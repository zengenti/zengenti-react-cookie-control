import React from "react";
export type CookiePopupProps = {
    popup: {
        text: string;
    };
};
declare const CookiePopup: ({ popup }: CookiePopupProps) => React.JSX.Element | null;
export default CookiePopup;
